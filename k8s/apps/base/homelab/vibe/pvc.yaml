apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: vibe-data
  namespace: vibe
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 20Gi
  storageClassName: longhorn
