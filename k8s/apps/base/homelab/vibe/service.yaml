apiVersion: v1
kind: Service
metadata:
  name: vibe
  namespace: vibe
  labels:
    app: vibe
spec:
  selector:
    app: vibe
  ports:
    - name: opencode
      port: 3000
      targetPort: 3000
      protocol: TCP
